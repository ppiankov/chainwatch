name: clickhouse_replication_lag
runbooks:
  - clickhouse
finding_types:
  - clickhouse_replication_lag
  - process_anomaly
  - network_anomaly
triggers:
  - replication lag
  - replication queue
  - lagging replica
  - queue depth
severity: high
remediation_type: config
title: "Resolve ClickHouse replication lag on {{.Scope}}"
description: |
  Finding: {{.Detail}}
  Reduce replication queue depth and recover lagging replicas to restore healthy replication.
