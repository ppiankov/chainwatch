# Agent execution classes

Chainwatch does not target specific AI products.
It targets recurring execution patterns used by modern AI agents.

Different vendors expose different APIs, but they fall into a small
number of architectural classes.
